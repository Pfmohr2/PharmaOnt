# services/workflow

Review, approval, validation preview, release staging, promotion, and rollback state machine.

User-initiated semantic changes must pass through this service rather than direct SPARQL updates.

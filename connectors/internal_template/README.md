# connectors/internal_template

Internal source template connector target directory.

Internal sources default to `internal_confidential` and may elevate to `contains_phi_or_pii`. Unknown sensitivity or license posture fails closed.

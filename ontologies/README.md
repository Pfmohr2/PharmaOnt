# ontologies

Version-controlled semantic assets for PharmaOps.

- `core/`: OWL/RDF ontology modules.
- `shapes/`: SHACL validation shapes.
- `mappings/`: SSSOM-like mapping files and governed mapping fixtures.
- `fixtures/`: small ontology/data fixtures for tests.

# apps

Application frontends for PharmaOps.

- `web/`: main workbench for search, entity pages, mapping registry, evidence review, release dashboard, audit browser, connector status, and admin workflows.
- `admin/`: optional internal administration console when admin workflows need isolation from the main workbench.
